---
trigger: always_on
---

1. A TextButton component has no text argument, it has a content argument, which consists of a component, like ft.Text("something")
2. When executing flet run, always make sure to enter the .venv